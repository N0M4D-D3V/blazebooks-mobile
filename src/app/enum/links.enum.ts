export enum Email {
  Support = "support.community@blaze.book",
}

export enum SocialMedia {
  FB = "#",
  IG = "#",
  YT = "#",
  Web = "#",
  PayPal = "#",
  Coffee = "#",
}

export enum Links {
  API = "http://localhost:3000/api/v1",
}
