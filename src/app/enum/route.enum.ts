export enum RouteName {
  Home = "home",
  Search = "search",
  Config = "config",
  Reader = "reader",
}

export enum RoutePath {
  Home = "/home",
  Search = "/search",
  Config = "/config",
  Reader = "/reader",
}
