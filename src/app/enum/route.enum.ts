export enum RouteName {
  Home = "home",
  Search = "search",
  Config = "config",
  Reader = "reader",
  Community = "community",
}

export enum RoutePath {
  Home = "/home",
  Search = "/search",
  Config = "/config",
  Reader = "/reader",
  Community = "/community",
}
