export enum RouteName {
  Login = 'login',
  Home = 'home',
  Search = 'search',
  Detail = 'detail',
  Reader = 'reader',
}

export enum RoutePath {
  Login = '/login',
  Home = '/home',
  Search = '/search',
  Detail = '/detail',
  Reader = '/reader',
}
