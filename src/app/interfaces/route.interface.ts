export enum RouteName {
  Login = 'login',
  Home = 'home',
  Search = 'search',
  Reader = 'reader',
}

export enum RoutePath {
  Login = '/login',
  Home = '/home',
  Search = '/search',
  Reader = '/reader',
}
